import styles from "./styledBoxes.module.css"

const StyledBox = (props) => {

    return (
        <div className={styles.boxedStyle} > 
            Hola
        </div>
    )
}

export default StyledBox;